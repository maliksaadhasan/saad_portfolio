import ProcessSection from "@/app/components/ProcessSection";
import SectionPage from "@/app/components/SectionPage";

export default function Process() {
  return (
    <SectionPage
      title="My Process | How I Run Paid Media Campaigns"
      description="The step-by-step process behind every account I run: audit, tracking setup, creative testing, scaling and reporting - so you know exactly what happens after we start."
      canonical="https://www.maliksaadhasan.com/process"
    >
      <ProcessSection />
    </SectionPage>
  );
}
